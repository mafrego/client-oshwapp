import { createLocalVue, mount, shallowMount } from '@vue/test-utils'
import PageHeader from '@/components/PageHeader.vue'

xdescribe('PageHeader', () => {
  it('test set up corretly', () => {
    expect(true).toBe(true)
  })
})